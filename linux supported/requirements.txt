# Vibe Coding Agent - Dependencies
# Copyright (c) 2026 xingluosama

# ── Core ──
pywebview>=4.0
openai>=1.0
anthropic>=0.30
keyring>=24.0
requests>=2.28

# ── Platform-specific ──
# Windows: DPAPI encryption (built-in via pywin32)
pywin32>=305; sys_platform == 'win32'
# Linux/macOS: Fernet symmetric encryption
cryptography>=41.0; sys_platform != 'win32'

# ── Optional: Docker sandbox ──
docker>=7.0

# ── Optional: System monitoring ──
psutil>=5.9

# ── Optional: Document parsing ──
PyPDF2>=3.0
python-docx>=1.0
openpyxl>=3.1
